{
  "title": "Which function doesn't work with ArrayAccess? e.g. $object implements ArrayAccess",
  "choices": {
    "a": "$object->offsetExists('foo')",
    "b": "array_key_exists('foo', $object)",
    "c": "isset($object['foo'])"
  },
  "answers": ["b"]
}
